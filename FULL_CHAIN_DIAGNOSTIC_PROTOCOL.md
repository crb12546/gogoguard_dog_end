# Go2 路线录制与巡检全链路体检规程

## 这次要回答的不是一个问题，而是逐层排除

一次“录制一遍、执行一遍”必须能够分别回答：

1. 狗在地面上的真实路线是什么；
2. 雷达、IMU 和 FAST-LIO 当时认为狗在哪里；
3. 录制器拿到了哪些里程计帧，如何按 0.40 m 写成 CSV；
4. CSV 在保存、上传、下载过程中是否一字未变；
5. 人工起点锚定把原始 CSV 变换到本次坐标系时是否算对；
6. 跟线器基于哪一帧、哪个最近点和目标点算出了什么命令；
7. 安全节点是否因点云、超时或障碍物改写了命令；
8. ROS 命令经过 UDP 后是否丢包、延迟或被限幅；
9. Unitree 底层实际报告的速度、姿态、脚力和电机状态是否跟随命令；
10. CPU、功耗模式、频率、温度、内存、磁盘、网络或调度是否与异常同时发生；
11. 如果车载定位认为自己走对了，狗在真实地面上是否仍然走偏。

第 11 项不能由任何一组车载日志独立证明。雷达里程计、控制器和路线图都可能共享同一个错误坐标。必须保留固定机位视频和地面参照物作为外部真值。

## 已布置的证据链

| 环节 | 证据 |
|---|---|
| 雷达网络和完整帧 | Livox 驱动内部 `LIVOX_STREAM_HEALTH`、发布时延、网卡包/错包/丢包 |
| FAST-LIO 输入队列与输出 | `FAST_LIO_INPUT_TIMING`、`FAST_LIO_OUTPUT_TIMING`、完整 `/Odometry` rosbag |
| FAST-LIO 活性 | `FAST_LIO_RECOVERY`、健康失败和永久锁死事件逐次计数；rosbag 直接从 SQLite 时间戳计算最大里程计间隔 |
| IMU 与姿态 | `/livox/imu` rosbag及限频 JSONL，完整四元数、roll/pitch/yaw |
| 雷达定位与狗身姿态一致性 | 按时间配对 FAST-LIO 四元数与 SportModeState 狗身 IMU 四元数，统计相对 roll/pitch/yaw 及其全程变化 |
| 录制输入 | `/Odometry` rosbag、20 Hz 独立里程计副本、20 Hz 遥控器摇杆 |
| CSV 生成 | 原始 CSV、间距/曲率审计、用遥测重新执行 0.40 m 抽样的逐点误差 |
| 文件身份 | CSV SHA-256；录制证据 sidecar；巡检启动前再次校验 |
| 坐标会话 | FAST-LIO 的 boot ID、PID、进程启动 tick；录制首尾和巡检首尾均保存 |
| 坐标变换 | 原始 CSV、运行时 CSV、完整人工锚点四元数和刚体变换；自动逐点复算 |
| 跟线决策 | 不替换当前控制数学的诊断包装层；每 50 ms 记录实际使用的里程计回调序号/时间戳、最近点、目标点、横向误差、航向误差和原控制器实际发布的命令 |
| 安全改写 | `/patrol_cmd` 与 `/cmd_vel` 同时记录，安全原因、点云年龄、障碍点数 |
| UDP/SDK | 发送/接收序号、间隔、传输时延、丢序号、`Move` 调用耗时 |
| 狗底层响应 | SportModeState 速度/位置/yaw rate；LowState 电池、功率、脚力、电机温度 |
| 计算平台 | 每核利用率/频率/在线状态、进程线程/亲和性/各线程实际运行核、GPU/EMC、功耗轨 |
| 系统压力 | NVPModel、温度、内存、PSI、磁盘 I/O、网络、UDP 内核计数、调度抖动 |
| 代码和配置 | 开始/结束系统快照，关键源码/可执行文件 SHA-256，FAST-LIO 与 Livox 源文件、安装文件及运行时参数 |
| 真实地面轨迹 | 固定机位手机视频、地面起点中心、朝向线及沿途可量测标记 |

为避免“观察者效应”，Python 不额外反序列化完整点云。完整点云一帧很大，额外订阅可能恰好制造我们要测的性能问题。点云链路由驱动和 FAST-LIO 进程内部打点证明；IMU、里程计、狗状态和控制命令体积小，才进入独立遥测和 rosbag。证据采集进程以较低调度优先级运行；它们启动后还必须经过连续里程计帧和系统 CPU 稳定门禁，正式录制或运动才会放行。启动热身日志与正式路线日志分开保存，避免把采集器刚启动的瞬态误判成路线过程故障。

## 测试前一次性准备

1. 充足电量，雷达支架锁紧。雷达可以保持现在的固定倾斜，但录制和执行之间绝对不能移动；后续会用配置中的外参和姿态数据核验倾斜是否被正确建模。
2. 本轮固定一个功耗模式。为了先排除算力上限，建议使用 25W；不要在录制和执行之间切换。系统会保存实际 NVPModel、8 核在线状态、每核频率和功耗轨。15W/25W 的严格 A/B 应在路线问题定位后另做。
3. 不在录制与执行之间修改代码、控制参数、雷达外参或安全阈值。
4. 在地面标出：
   - 狗几何中心的起点；
   - 四脚大致落点或机身矩形；
   - 从中心向前至少 1 m 的机头朝向线；
   - 第一段每隔约 1 m 的可见参照点。
5. 固定一台手机，画面至少覆盖起点、第一段长路径和第一个转弯。不要手持跟拍。建议 30 或 60 fps，并保证地面标记可见。
6. 手机先开始录像；狗在起点静止至少 5 秒。

仅把狗“摆得很近”不够。位置可以做到厘米级，但 1° 初始朝向误差在 10 m 后横向就是约 17 cm，2° 约 35 cm。1 m 朝向线可以把这一项变成可量测证据。

视频与车载日志用“第一次非零运动开始”的画面和命令时间对齐；不要剪辑或变速原视频。

## 第一步：录制新路线

1. 打开 `http://127.0.0.1:8642`，确认底座、Livox、FAST-LIO 和里程计正常。
2. 输入一个全新的路线名，点击“开始录制”。
3. 管理台会先完成：
   - 采集器启动前的静止状态和 FAST-LIO 会话记录；
   - 系统、功耗和代码快照；
   - 基础传感器遥测就绪检查；
   - 逐核性能监控；
   - `/Odometry`、IMU、遥控器和 TF rosbag；
   - 等待 CPU 降到门限内并取得连续、足够新的 FAST-LIO 帧；
   - 重新取得正式录制起点和会话，并切分启动/正式日志；
   - 定位会话保护。
4. 原版 0.40 m 定距录制器会先完成订阅，但 CSV 写入门禁保持关闭；只有所有证据源和连续新鲜里程计都就绪后，门禁才允许当前新鲜回调成为第 0 个路线点。界面明确显示“路线录制已开始”后再遥控狗走路线。
5. 到终点后保持静止至少 5 秒，再点击“停止录制”。
6. 等界面明确显示“路线与完整证据包已保存”。如果显示“有警告”，先不要执行。

停止时会自动：

- 正常关闭路线录制器和 rosbag；
- 再取一次定位会话与系统快照；
- 复制 Livox、FAST-LIO 和底座健康日志；
- 审计 CSV 点距、转弯和 Z/roll/pitch；
- 用独立里程计副本重新执行 0.40 m 抽样；
- 从 rosbag 数据库计算正式录制窗口内的里程计最大间隔；
- 统计正式窗口内的 FAST-LIO 恢复、健康失败和永久锁死事件；
- 计算 SHA-256，并生成 `<路线>.csv.recording.json`。

少于 5 个航点、总长不足 1 m、存在大于 1 m 的航点断层、正式窗口内里程计记录间隔超过 350 ms，或出现 FAST-LIO 恢复/健康失败/锁死事件，都会把路线标为“有警告”；这类 CSV 不会被完整巡检入口接受。

## 第二步：执行同一条路线

1. 不重启 FAST-LIO，不切功耗模式，不改代码和参数。
2. 把狗中心放回地面中心标记，机头严格对准 1 m 朝向线，静止至少 5 秒。
3. 固定手机继续录像。
4. 二选一启动：
   - 管理台“启动完整巡检”；
   - GoGoGuard 下发 `start_patrol`。

两者现在调用同一套生产巡检和同一套黑匣子。不要先点“安全单节点·调试”；完整巡检会自行启动安全节点、UDP 桥和视频。

本轮诊断包装层会先启动 ROS 参与者、订阅里程计并持续发布零，但此时不调用原控制循环，避免在热身等待中推进原控制器的卡住/反向状态。收到至少 5 个新鲜里程计回调，并再次通过 CPU 与连续 FAST-LIO 帧门禁后，才原子释放运动；释放后调用机器狗当前部署的 Go2_2 控制回调，控制参数和数学保持不变，包装层只截取交给 ROS 发布器的实际 `Twist` 并写入证据文件。这样既不把订阅启动峰值混入第一次运动，也不会把一份尚未部署的控制算法改动混入基线实验。

5. 人站在急停可及位置。不要为了“帮它走对”而中途扶正或移动雷达。
6. 结束、失败或人工停止后，都要再执行一次“停止巡线”或 GoGoGuard `stop_patrol`，等待证据收尾和自动报告生成。

如果失败发生在起点锚定或启动门控阶段，系统会自动收尾该次失败证据，且只会收尾本次新建目录，不会误停一条原本已在运行的巡检。定位会话保护触发后也会保留实验指针，供同一套停止流程生成报告。

## 跑完后不要立刻做的事

- 不要重跑覆盖现场；
- 不要修改 CSV；
- 不要重启底座来“试试看”；
- 不要先关狗。

先保留手机原视频，并等本地下载完成。每次巡检目录会生成：

- `route_original.csv`
- `route_runtime.csv`
- `route_recording.json`
- `manual_anchor.json`
- `experiment_telemetry.jsonl`
- `follower_control_trace.jsonl`
- `performance_monitor.log`
- `rosbag/`
- Livox/FAST-LIO/跟线/安全/UDP 日志
- `system_start.json` / `system_end.json`
- `experiment_audit.json`
- `experiment_audit.md`

## 自动报告如何定位责任层

- 录制重算与 CSV 不一致：录制订阅/抽样层；
- 原始 CSV 到运行时 CSV 无法逐点复算：坐标变换层；
- 运行时 CSV 正确，但 FAST-LIO 轨迹相对路线横向误差大：跟线决策、安全改写或执行层；
- `follower_control_trace.jsonl` 会直接给出每个控制周期用了哪个里程计回调；10 Hz 里程计被 20 Hz 控制循环连续使用约 2 次是正常的，风险看的是时间戳年龄和连续复用时长，而不是“丢掉中间帧”这个动作本身；
- `/patrol_cmd` 正确而 `/cmd_vel` 不同：安全节点；
- `/cmd_vel` 正确而 UDP 有丢序号/大延迟：传输层；
- UDP 命令正确而 SportModeState 速度明显不跟：Unitree 执行、打滑或机体层；
- FAST-LIO 轨迹与 CSV 很接近，但固定视频显示真实地面偏离：定位/外参/退化问题；
- FAST-LIO 与狗身 IMU 的相对姿态在一次运行中明显变化：雷达支架松动、时间配对异常或两套姿态源不一致；相对姿态稳定只能证明安装/姿态源一致，不能代替地面真值；
- 车载与视频都只表现为一开始就整体旋转：初始朝向摆放或人工锚定问题；
- 异常与某核 100%、降频、温度、PSI 或雷达队列同时出现：计算/调度层。

固定雷达倾斜并不天然“无所谓”。如果外参准确而且安装完全不动，录制和执行可以一致；如果外参中的旋转与真实安装角不符，车辆转向、地面起伏或姿态变化会把高度误差耦合进 XY。此次完整四元数、Z/roll/pitch、点云内部时序和外部视频正是用来区分这件事。
