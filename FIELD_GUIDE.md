# Go2 现场操作手册

日常带狗外出调试/巡检的完整流程。基于 2026-07 实测沉淀。

## 当前已验证状态（2026-07-05）

```text
✅ 录线: route_recorder 可用，管理台录制时会实时显示正在写入的 CSV 路线
✅ 建图: xiaoqu1.pcd 已保存约 49 万点，PCD 采集链路可用
✅ 自动巡线主链路: waypoint_follower -> unitree_safe_cmd_node -> /api/sport/request 已验证能真实移动
⚠️ 长路线巡线: xiaoqu1.csv 可推进到长路线后段，但大角度转向处会出现原地转向/卡住，需要调 follower 参数
⚠️ 避障: 安全节点是前方窄 ROI 的速度门控，不是全身避障；参数仍需现场调
```

已保存的关键样本：

```text
roomtest7.csv: 短路线，验证 safe chain 能动
xiaoqu1.csv: 315 点，约 142 m，路线质量正常
xiaoqu1.pcd: 约 490,313 点，约 11.4 MB
```

## 出门清单

```text
□ 狗（电量 >60%，一次完整测试约耗 20-30%）
□ 遥控器（物理急停，全程拿手里）
□ Mac + USB 网线转接器 + 网线（备用链路）
□ iPhone（开热点）
```

## 网络速查

| 设备 | 地址 | 说明 |
|------|------|------|
| 狗(Orin) 家里/同网 WiFi | `192.168.0.122` | `ssh go2home`，当前家里调试主链路 |
| 狗(Orin) 热点IP | `172.20.10.2` | `ssh go2`，开机自动连热点 |
| 狗(Orin) 网线IP | `192.168.123.18` | `ssh go2wired`，Mac 网线口已配静态 192.168.123.222 |
| Go2 本体 | `192.168.123.161` | 狗内部网络，勿动 |
| MID-360S 雷达 | `192.168.1.161` | 狗内部网络，勿动 |
| Z-1Pro 摄像头 | `192.168.144.108` | Orin 上 `eth0` 已配 `192.168.144.100/24`，RTSP/GCU 均从 Orin 访问 |

- 家里调试优先用 `ssh go2home`；现场用 `ssh go2` 热点；有线兜底用 `ssh go2wired`
- 热点没设备连接时会休眠 → 现场开机顺序：先开热点（停在热点设置页），再开狗；狗连上后热点不再休眠
- macOS 会自动跳到自家 iPhone 热点，注意 Mac 和狗要在同一个网
- `ubuntu.local` 在热点下**不可用**（它通告内部网卡 IP），用 `ssh go2`

## 标准流程

### 0. 开机与连接

```text
1. 家里: 狗和 Mac 连同一个 WiFi，确认 ssh go2home 可用
      现场: 手机先开热点并停在"个人热点"设置页，再开狗，Mac 连同一个热点
2. 开狗后等它站稳，网页/SSH 看到 Orin 在线
3. 启动平台: cd tools/patrol_console && nohup python3 server.py > /tmp/patrol_console.log 2>&1 < /dev/null &
4. 浏览器: http://127.0.0.1:8642 → 左上角"SSH 已连接"
5. 需要视频时，Z-1Pro 状态应显示网络/RTSP/云台均可达
```

网线备用：插线即可，平台自动优先走有线。

### 1. 启动底座（雷达+FAST-LIO，不会动狗）

```text
把狗摆到起点位置(如需回放旧路线，必须与录制时同点同朝向)
→ 点「启动 雷达+FAST-LIO」→ 狗保持静止 10 秒
→ 等底座状态行变绿「✅ 运行中, 定位正常」
```

**FAST-LIO 坐标系诞生于启动那一刻，但现在不需要为了把坐标“清零”而重启底座。**
录制 CSV 时保存路线几何；启动巡线时，程序会把原 CSV 第 0 点刚性变换到狗当前的
静止位姿，并同时对齐朝向。PCD 与 CSV 可以在不同的 FAST-LIO 会话中分别录制。

前提是操作员必须把狗放回路线的真实物理起点，并让机头方向与录制开始时一致；
没有启用 PCD 重定位时，程序无法识别“摆错了 5 米”这种物理错误。任务开始前可以
重启 Livox/FAST-LIO，任务进行中则不能重启；一旦定位会话变化，当前巡检或采集会
立即停止并判为无效。

### 2. 建图（可与录路线同时进行）

```text
点云地图起名 → 「开始采集」→ 状态行显示实时点数
→ 遥控带狗把区域走一遍 → 「结束保存」
→ 等右上角"✅ 点云已保存"通知 → 「载入点云」看俯视图 → 可「下载PCD」
```

### 3. 录路线

```text
路线起名 → 「开始录制」→ 等"✅ 已确认录制中"通知
→ 遥控带狗沿巡检路线走（0.2~0.5 m/s，转弯放慢）
→ 中间地图会实时显示两条线：蓝线是 Odometry 实时轨迹，灰/黄线是已经写入 CSV 的路线
→ 「停止录制」→ 通知会报保存的点数 → 路线自动进列表
```

### 4. 自动巡线（危险操作，人在狗旁）

```text
遥控狗回到路线起点附近 → 让狗站立
→ 选路线「载入查看」→ 下方对齐提示必须 ✅(<0.3m)
→ 勾选「解锁运动操作」
→ 「安全节点·真实」（首次可先「干跑」验证逻辑）
→ 「启动巡线」，长路线首测建议 0.20~0.25 m/s，确认稳定后再升速
→ 盯地图轨迹；异常立刻点「急停」或遥控器接管
```

急停原理：杀巡线节点，安全节点 0.5 秒内持续发零速度。

当前长路线调参建议：

```text
v_base/max_vx: 0.20~0.25 起步
loop_mode: pingpong 已验证；once 需要用 safe chain 单独公平测试
lookahead_distance: 可从 0.6 提到 0.8~1.0，减少大角度追点导致的原地转向
goal_distance: 短路线可降到 0.10，长路线可先保持 0.25
```

### 5. 收工

```text
「停止实时预览/停止分段」→「停止全部控制节点」→「停止底座」
→ 需要的路线/PCD/视频「下载」到 Mac
→ 确认远端进程灯全灭（雷达、FAST-LIO、录制、点云、视频分段均空闲）
→ 关狗 → 关热点/收线
```

### 6. 下次开机快速检查

```text
1. 启动本地管理台并确认左上角 SSH 已连接
2. 左栏感知设备: MID-360、Z-1Pro 网络、RTSP、云台均为可达/打开
3. 点「探测」确认 Z-1Pro RTSP + GCU 正常
4. 可点「测试实时播放」验证 20 秒延迟预览；测试完点「停止实时预览」
5. 再按标准流程启动底座、录路线/采点云
```

旧脚本录出的部分 MP4 可能不可播放；2026-07-05 15:24 后的新视频已修复为可播放 H.264/MP4，并支持浏览器 Range 预览。

## 故障速查

| 现象 | 原因与处理 |
|------|-----------|
| 页面"SSH 断开" | 热点休眠/Mac 跳网了。确认手机热点开着、Mac 连的是热点；约 15s 自动恢复 |
| 点开始录制提示需要定位 | 先启动底座并等状态变绿 |
| ALREADY_RUNNING 提示 | 真的有同类进程在跑（防重复保护），看进程指示灯 |
| 人工起点锚定失败 | 让狗保持静止，确认 FAST-LIO 有连续里程计后重试；同时确认狗真实位于录制起点且机头方向一致 |
| BASE_RESTART_BLOCKED_ACTIVE_MODE | 正在录制、采 PCD、巡检或运行控制节点。先正常停止当前任务，再重启底座 |
| LOCALIZATION_SESSION_ABORTED | 任务期间 FAST-LIO 重启或里程计中断，当前任务/输出已判无效；恢复底层后从真实起点重新开始 |
| 平台页面整体冻住 | Mac 上服务被挂起（罕见，已修）。重启: `pkill -9 -f server.py` 后按第 0 步重启 |
| WiFi 信号显示"弱" | 狗离手机太远，考虑插网线或人跟上去 |
| 红灯闪/狗趴下 | 先物理检查（电池/温度/异味），参考主 README 与售后；勿急着重启跑程序 |

## 数据位置

```text
狗上: 路线 /home/unitree/go2_fastlio_ws/src/go2_fastlio_patrol/routes/*.csv
      点云 /home/unitree/go2_fastlio_ws/maps/console/*.pcd
      视频 /home/unitree/go2_fastlio_ws/patrol_logs/videos/*.mp4
Mac:  代码副本 orin_go2_fastlio_ws/   平台 tools/patrol_console/
      平台日志 /tmp/patrol_console.log
```

## 安全红线

1. 遥控器永远在手里，它是最高优先级的急停
2. 自动巡线时人不离狗，首测速度 ≤0.10 m/s、短路线
3. 「安全节点·真实」启动后，`/api/sport/request` 通路即打开——不用时点「停止全部控制节点」
4. 电量 <25%（左栏变红）停止测试
5. 关节温度 >80℃（红色）立即停下休息
